server:
  port: 8102

spring:
  application:
    name: wei-test-cloud
  cloud:
    consul:
      # consul的IP
      host: 192.168.11.128
      # consul启动端口
      port: 8500

  profiles:
    active: dev